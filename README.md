# Bash Encrypter & Decrypter
```
$ apt update && apt upgrade
$ pkg install python2
$ pkg install git
$ pkg install nodejs
$ npm install -g bash-topsh
$ cd /sdcard
$ git clone https://github.com/topenkorangasing/obfuscate
(masukan sh file yang ingin di encrypt atau decrypt di folder "obfuscate"
$ cd obfuscate
$ python2 bash.py
```
